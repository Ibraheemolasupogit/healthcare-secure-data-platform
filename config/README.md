# Configuration

Commit non-secret defaults and schemas only. Runtime settings use the `HEALTHCARE_PLATFORM_` prefix. External credentials come from protected environment variables or secret managers and must be validated without printing values.
