# Model directories

Directories represent STAGING, INTERMEDIATE, CURATED, MART and SEMANTIC responsibilities described in `docs/architecture/data-layers.md`. Models are intentionally absent until Milestones 5–6; add a directory-level YAML file when the first real model is introduced.
