# Audit-event model

The audit-event catalogue requires timestamp, actor reference, purpose, policy ID and
correlation ID where applicable. Events prohibit patient payloads and secret values.

Events cover authentication attempts, privileged-role use, policy changes, data export,
sensitive-object access, failed access, model approval, feature approval, semantic-model
promotion and evidence generation.
