# Feature quality

Quality expectations include:

- no point-in-time leakage;
- unique entity-observation grain;
- populated lineage;
- null-rate monitoring;
- freshness thresholds;
- schema compatibility.

Freshness states are `FRESH`, `WARNING`, `STALE`, `UNAVAILABLE` and `NOT_APPLICABLE`.
