# Incident model

The incident model defines categories, severity levels, lifecycle states and
allowed transitions.

Severity considers recovery tier, blast radius, sensitivity, financial value at
risk, duration, dependency impact, governance impact, recovery readiness and
user-facing impact. It does not classify clinical severity.

The deterministic incident simulation produces local evidence only and never
creates alerts, tickets or notifications.
