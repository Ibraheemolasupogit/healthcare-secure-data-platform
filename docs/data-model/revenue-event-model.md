# Revenue event model

`fct_revenue_event` classifies source revenue events as governed financial movements.

The model uses event classes such as billing lifecycle, cash collection, refund, adjustment and write-off events. It preserves source amount and exposes a governed signed amount.

The output must not be described as recognised revenue. Formal accounting recognition rules are out of scope.

