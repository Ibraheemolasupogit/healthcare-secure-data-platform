# Airflow recovery

Airflow recovery covers DAG bundle restoration, metadata database design, connection and
secret boundaries, scheduler/webserver order, paused-DAG default and controlled catch-up.

Milestone 16 does not deploy an Airflow scheduler or managed Airflow service.

