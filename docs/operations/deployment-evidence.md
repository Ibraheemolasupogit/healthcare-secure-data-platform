# Deployment evidence

Deployment evidence lives under `deployment/reference/` and includes release and deployment
manifests, Terraform validation metadata, policy-gate output, artefact inventory,
promotion matrix, drift report, rollback plan, validation reports and checksums.

Evidence is local, synthetic, statically validated, not deployed, not applied and does not
use live credentials.

