# Feature deprecation

Allowed lifecycle statuses are `DRAFT`, `VALIDATED`, `APPROVED`, `ACTIVE`, `DEPRECATED`, `RETIRED` and `BLOCKED`.

Deprecation requires:

- successor or retirement reason;
- consumer impact assessment;
- migration window;
- exact affected feature-set versions.

Repository approval is local portfolio governance only.
