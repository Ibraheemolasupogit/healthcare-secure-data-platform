# Health evaluation

Run:

```bash
healthcare-platform operations evaluate-health
```

The command evaluates local synthetic health from the operations registry. It
does not probe Snowflake, Airflow, Dataiku, Fabric, Power BI, cloud monitoring
or any external endpoint.
