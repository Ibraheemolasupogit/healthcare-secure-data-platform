# Recovery evidence

Recovery evidence is generated under `recovery/reference/` and includes region registry,
residency policy, recovery tiers, dependency graph, scenarios, failover decision report,
recovery manifest, validation report, failback plan, summary and checksums.

Evidence is synthetic, local, not deployed, not live tested, not legal advice and not
certification evidence.

