# Release management

Release conventions use milestone tags, release candidates and semantic production tags
where organisational release management supports them. A release manifest must reference
commit SHA, artefacts, checksums, compatibility notes, deployment status, approval status,
rollback reference and limitations.

Milestone 15 does not create a GitHub release automatically.

