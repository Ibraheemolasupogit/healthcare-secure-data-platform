# Rollback

Rollback is a reviewed procedure, not an automatic action. Terraform, dbt, Airflow,
Dataiku, feature-store, Fabric/Power BI and governance rollback targets are declared in the
deployment registry.

Every rollback requires approval, compatibility review, evidence capture and post-rollback
validation. Production rollback was not executed.

