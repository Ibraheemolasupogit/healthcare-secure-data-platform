# Sensitive-field coverage

Sensitive-field coverage is generated into `governance/reference/sensitive_field_coverage.csv`.
The report checks that direct identifiers, quasi-identifiers, financial references,
evidence references and secret metadata have masking policy coverage.
