# Milestone 1 has no provider-backed resources, so credential-free validation
# deliberately requires no provider download. Copy the reviewed example in
# providers.tf.example into active configuration when Milestone 3 begins.
