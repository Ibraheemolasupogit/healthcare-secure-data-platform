terraform {
  required_providers {
    snowflake = {
      source  = "snowflakedb/snowflake"
      version = "~> 1.0"
    }
  }
}

provider "snowflake" {
  # Configure account, user and authentication through protected environment
  # variables or workload identity. Never commit them here or in tfvars.
}
