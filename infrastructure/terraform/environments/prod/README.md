# Production environment

Production configuration is intentionally absent. It will use isolated remote state, protected apply approval, a production deployment role, policy checks and immutable plan evidence. Never copy development credentials or state into this directory.
