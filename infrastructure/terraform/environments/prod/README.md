# Production environment

This root declares PROD names and sizing from the shared foundation contract. It must use isolated remote state, protected approval and environment-scoped provider authentication. Local `init` and `validate` do not deploy anything. No production apply is automated in Milestone 3.
